bundle.js
bundle.js.map
dist/
node_modules/

# Internal design docs — kept local; excluded from the public `publish` branch / origin/main.
PLAN.md
docs/DEVIATIONS_FROM_MPT.md
# The @bsv/sdk serialization-cache report — goes to the SDK developers DIRECTLY, not published here
# while it is unfixed. sun-dive/PharLap is a public repo.
docs/SDK_SERIALIZATION_CACHE_BUG.md
LISTING_SITE_PLAN.md

# build artifacts
smart-nfts-app.zip
*.zip

# Regenerable PNG renders of the SVG diagrams (build output; SVGs are the source of truth)
docs/diagrams/png/

# Local high-res source art (fal.ai masters etc.) — kept local, never deployed.
# Export the optimized .jpg you actually use into brand/home/.
art-src/

# BigRed curation tool (private)
# imageSamples.ts is part of THIS, not of the app: Node-only (spawnSync + ImageMagick), never bundled,
# imported by nothing but its own test. Its test travels with it or a fresh clone fails `npm test`.
src/imageSamples.ts
test/imageSamples.test.ts
curate-bigred.ts
curate-publish.sh
build-curator.sh
SERVER_CURATION.md
VIDEO_SCRIPTS.md

# OG preview cache (created at runtime on the server)
/og/
/links/
